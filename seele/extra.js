const sle = require('./seele')
const fs = require('fs-extra')
const path = require('path')

async function autodeploy(dir){
  // 
}

autodeploy()

async function contractOrder(){
  
}
// async function compile(){}

// async function order(){}

module.exports = {
  web: {
    fill: fillTransactionLimitAndNonce,
    send: sendTransaction,
    // nonceOf:
  },
  off: {
    // shardOf:
    // pubadOf:
    // keyflOf:
  }
}

async function fillTransactionLimitAndNonce(limit, nonce){}

async function sendTransaction(){}

async function wait(hash, progress, heightDifference){
  
}